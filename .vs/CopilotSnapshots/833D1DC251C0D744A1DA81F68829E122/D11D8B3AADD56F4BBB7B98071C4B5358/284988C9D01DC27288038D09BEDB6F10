# BirdTaxonomyDbContext - Contexto para Codex / ChatGPT

Ubicación del archivo DbContext en el proyecto:
- BirdTaxonomy.API/Persistence/BirdTaxonomyDbContext.cs
- Clase: BirdTaxonomy.API.Persistence.BirdTaxonomyDbContext

Cómo se registra en el proyecto (Program.cs):
- Archivo: BirdTaxonomy.API/Program.cs
- Clave de configuración esperada en appsettings.json: ConnectionStrings:BirdTaxonomyDb

Servidor y base de datos solicitados por el usuario:
- Server: (localdb)\\MSSQLLocalDB
- Database: Ave

Proveedor recomendado:
- Microsoft.EntityFrameworkCore.SqlServer

Cadena de conexión (valor esperado en appsettings.json):

Server=(localdb)\\MSSQLLocalDB;Database=Ave;Trusted_Connection=True;MultipleActiveResultSets=true

Ejemplo mínimo de appsettings.json:

```json
{
  "ConnectionStrings": {
    "BirdTaxonomyDb": "Server=(localdb)\\MSSQLLocalDB;Database=Ave;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
```

Snippet mínimo para Program.cs (registro del DbContext):

```csharp
var connectionString = builder.Configuration.GetConnectionString("BirdTaxonomyDb");
builder.Services.AddDbContext<BirdTaxonomyDbContext>(options =>
    options.UseSqlServer(connectionString));
```

Paquetes NuGet necesarios:
- Microsoft.EntityFrameworkCore.SqlServer
- Microsoft.EntityFrameworkCore.Design (opcional para herramientas de diseño)

Comandos útiles (CLI):
- dotnet add package Microsoft.EntityFrameworkCore.SqlServer
- dotnet tool install --global dotnet-ef
- dotnet ef migrations add InitialCreate
- dotnet ef database update

Propósito de este archivo:
Proveer a Codex/ChatGPT la información mínima y directa para localizar y entender la configuración de conexión del DbContext en este proyecto. Coloque este archivo en la raíz del proyecto para que herramientas que inspeccionan el árbol de archivos lo encuentren fácilmente.
